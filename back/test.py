# from classes.structs.Command import Command

# @Command(
#     name="test",
#     description="A test command.",
#     how_to_use="j!test",
#     aliases=[]
# )
# async def test_command(client, message, args, profile, logger, guild, interfacer=None, used_name=None):
#     """
#     A simple test command.
#     """
#     await message.reply(f"client: {client}, message: {message}")
#     logger.debug(f"Command 'test' executed by {message.author.id} in guild {guild.id}")

# # Expose the command for the loader
# default = [test_command]
